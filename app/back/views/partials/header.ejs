<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/picture/favicon/favicon.ico" />
    <link rel="stylesheet" href=" /css/style.css">
    <link rel="stylesheet" href="/css/category.css">

    <title><%= homeName %></title>
</head>
<body class="body">
    <header class="header">
        <div class="logo">
            <img src="/picture/logo/O'Frigo.png" alt="">
            <a class="logo_span" href="#">O'FRIGO</a>
        </div>
        <div class="title">
            <h1>Dashboard</h1>
        </div>
        <div class="profil">
            <% if (homeName != 'LoginPage') { %>
                <a href="#"><%=user.last_name%> <%=user.first_name%></a>
            <%} %>
        </div>
    </header>
    <% if(homeName != 'LoginPage') { %>
        <%- include('leftMenu') %>
    <% } %>



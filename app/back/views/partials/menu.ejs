<nav class="nav">
  <ul>
      <li>
        <a href="/admin/home">Accueil</a>
      </li>
      <% for(const name of menu) { %>
          <li>
            <a href="/admin/<%= name %>"><%= name.charAt(0).toUpperCase() + name.slice(1); %></a>
          </li>
      <% } %>
      <li>
        <a href="/admin/logout">Logout</a>
      </li>
  </ul>
</nav>

  